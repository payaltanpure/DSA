2130. Maximum Twin Sum of a Linked List
Medium
Topics
premium lock icon
Companies
Hint
In a linked list of size n, where n is even, the ith node (0-indexed) of the linked list is known as the twin of the (n-1-i)th node, if 0 <= i <= (n / 2) - 1.

For example, if n = 4, then node 0 is the twin of node 3, and node 1 is the twin of node 2. These are the only nodes with twins for n = 4.
The twin sum is defined as the sum of a node and its twin.

Given the head of a linked list with even length, return the maximum twin sum of the linked list.

 

Example 1:


Input: head = [5,4,2,1]
Output: 6
Explanation:
Nodes 0 and 1 are the twins of nodes 3 and 2, respectively. All have twin sum = 6.
There are no other nodes with twins in the linked list.
Thus, the maximum twin sum of the linked list is 6. 
Example 2:


Input: head = [4,2,2,3]
Output: 7
Explanation:
The nodes with twins present in this linked list are:
- Node 0 is the twin of node 3 having a twin sum of 4 + 3 = 7.
- Node 1 is the twin of node 2 having a twin sum of 2 + 2 = 4.
Thus, the maximum twin sum of the linked list is max(7, 4) = 7. 
Example 3:


Input: head = [1,100000]
Output: 100001
Explanation:
There is only one node with a twin in the linked list having twin sum of 1 + 100000 = 100001.














What is a Twin Node?

For a linked list of even length n, the twin of node i is:

Twin(i)=n−1−i

where i ranges from 0 to (n/2)-1.

In simple words:

First node ↔ Last node
Second node ↔ Second last node
Third node ↔ Third last node
and so on...
Example 1

Linked List:

Index:  0  1  2  3
Value: [5, 4, 2, 1]
Find Twins
Node Index	Twin Index	Values	Twin Sum
0	3	5 + 1	6
1	2	4 + 2	6

Maximum Twin Sum = 6

Example 2

Linked List:

Index:  0  1  2  3
Value: [4, 2, 2, 3]
Find Twins
Node Index	Twin Index	Values	Twin Sum
0	3	4 + 3	7
1	2	2 + 2	4

Maximum Twin Sum = 7



Approach 1: Store values in an Array (Easy)
Algorithm
Traverse the linked list and store all node values in a vector.
Let n be the size of the vector.
Initialize maxSum = 0.
For every i from 0 to n/2 - 1:
Twin index = n - 1 - i

Calculate:

sum = arr[i] + arr[n-1-i]

Update:

maxSum = max(maxSum, sum)
Return maxSum.
Example
5 -> 4 -> 2 -> 1

Vector = [5,4,2,1]

i = 0
sum = 5 + 1 = 6

i = 1
sum = 4 + 2 = 6

maxSum = 6
Complexity
Time: O(n)
Space: O(n)


// /**
//  * Definition for singly-linked list.
//  * public class ListNode {
//  *     int val;
//  *     ListNode next;
//  *     ListNode() {}
//  *     ListNode(int val) { this.val = val; }
//  *     ListNode(int val, ListNode next) { this.next = next; }
//  * }
//  */
// class Solution {
//     public int pairSum(ListNode head) {

//         ArrayList<Integer> list = new ArrayList<>();

//         while (head != null) {
//             list.add(head.val);
//             head = head.next;
//         }

//         int n = list.size();
//         int maxSum = 0;

//         for (int i = 0; i < n / 2; i++) {
//             int sum = list.get(i) + list.get(n - 1 - i);
//             maxSum = Math.max(maxSum, sum);
//         }

//         return maxSum;
//     }
// }



// optimal solution

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) {
 *         this.val = val;
 *         this.next = next;
 *     }
 * }
 */
class Solution {
    public int pairSum(ListNode head) {

        // Step 1: Find middle
        ListNode slow = head;
        ListNode fast = head;

        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }

        // Step 2: Reverse second half
        ListNode prev = null;
        ListNode curr = slow;

        while (curr != null) {
            ListNode nextNode = curr.next; //save
            curr.next = prev; //reverse
            prev = curr; //move prev
            curr = nextNode;//move curr
        }

        // prev is head of reversed second half
        ListNode first = head;
        ListNode second = prev;

        int maxSum = 0;

        // Step 3: Calculate twin sums
        while (second != null) {
            int sum = first.val + second.val;
            maxSum = Math.max(maxSum, sum);

            first = first.next;
            second = second.next;
        }

        return maxSum;
    }
}



// Let's understand the entire solution from theory to code for LeetCode 2130. Maximum Twin Sum of a Linked List.

// Theory

// Given:

// 5 -> 4 -> 2 -> 1

// Length = 4

// Indexes:

// 0    1    2    3
// 5 -> 4 -> 2 -> 1

// Twin pairs are:

// 0 ↔ 3  => 5 + 1 = 6
// 1 ↔ 2  => 4 + 2 = 6

// Answer = 6

// Problem with Linked List

// In an array we can directly do:

// arr[i] + arr[n-1-i]

// But in a linked list:

// 5 -> 4 -> 2 -> 1

// we cannot directly access the last node.

// So we need another trick.

// Main Idea

// Split the list into two halves.

// 5 -> 4 | 2 -> 1

// Reverse the second half.

// 5 -> 4 | 1 -> 2

// Now the twins become aligned:

// 5 ↔ 1
// 4 ↔ 2

// We can traverse both halves together.

// Complete Algorithm
// Step 1: Find Middle

// Use Slow-Fast pointers.

// slow = head;
// fast = head;

// Move:

// slow = slow.next;
// fast = fast.next.next;

// When fast reaches the end:

// slow reaches middle
// Example
// 5 -> 4 -> 2 -> 1

// Initially:

// slow = 5
// fast = 5

// Move once:

// slow = 4
// fast = 2

// Move again:

// slow = 2
// fast = null

// Stop.

// Middle found:

// slow
//  ↓
// 2
// Step 2: Reverse Second Half

// Current second half:

// 2 -> 1

// Reverse it:

// 1 -> 2

// Using:

// prev = null;
// curr = slow;

// Loop:

// nextNode = curr.next;
// curr.next = prev;
// prev = curr;
// curr = nextNode;

// After reversing:

// prev
//  ↓
// 1 -> 2
// Why Reverse?

// Before reverse:

// 5 -> 4 -> 2 -> 1

// Twin:
// 5 ↔ 1
// 4 ↔ 2

// Hard to compare.

// After reverse:

// First Half  : 5 -> 4
// Second Half : 1 -> 2

// Now:

// 5 ↔ 1
// 4 ↔ 2

// are at the same positions.

// Step 3: Compare Twin Sums

// Create two pointers:

// first = head;
// second = prev;

// So:

// first
//  ↓
// 5 -> 4

// second
//  ↓
// 1 -> 2
// First Pair
// sum = first.val + second.val;
// 5 + 1 = 6

// Update maximum.

// Move:

// first = first.next;
// second = second.next;

// Now:

// 4 and 2
// Second Pair
// 4 + 2 = 6

// Maximum remains 6.

// Step 4: Return Answer
// return maxSum;

// Output:

// 6
// Complete Code Explanation
// public int pairSum(ListNode head)

// Function that returns maximum twin sum.

// Find Middle
// ListNode slow = head;
// ListNode fast = head;

// Both start from head.

// while (fast != null && fast.next != null)

// Run until fast reaches end.

// slow = slow.next;

// Move one step.

// fast = fast.next.next;

// Move two steps.

// After loop:

// slow

// points to beginning of second half.

// Reverse Second Half
// ListNode prev = null;
// ListNode curr = slow;
// while (curr != null)

// Reverse until end.

// Store next node:

// ListNode nextNode = curr.next;

// Reverse link:

// curr.next = prev;

// Move pointers:

// prev = curr;
// curr = nextNode;

// After loop:

// prev

// is the new head of reversed half.

// Compare Twin Sums
// ListNode first = head;
// ListNode second = prev;

// Initialize answer:

// int maxSum = 0;

// Loop:

// while (second != null)

// Calculate twin sum:

// int sum = first.val + second.val;

// Update answer:

// maxSum = Math.max(maxSum, sum);

// Move pointers:

// first = first.next;
// second = second.next;
// Return
// return maxSum;
// Dry Run Table

// For:

// 5 -> 4 -> 2 -> 1

// After reversing:

// First  : 5 -> 4
// Second : 1 -> 2
// first	second	sum	max
// 5	1	6	6
// 4	2	6	6

// Return:

// 6
// Complexity Analysis
// Finding Middle
// O(n)
// Reversing
// O(n)
// Calculating Twin Sums
// O(n)

// Total:

// O(n)
// Extra Space

// Only pointers are used:

// slow
// fast
// prev
// curr
// first
// second

// So:

// O(1)

// ✅ Time Complexity: O(n)
// ✅ Space Complexity: O(1)

// This is the most optimal solution and the one expected in interviews.